# Kredensial & data sensitif — jangan pernah masuk repo
.env
*.session
*.session-journal
*.db

# Python
__pycache__/
*.pyc
.venv/
venv/

# Packaging (teledrive.spec ikut repo — resep build)
build/
dist/

# Android (Buildozer)
mobile/.buildozer/
mobile/bin/
mobile/apicreds.py
# Disalin/di-generate CI dari root repo (android.yml) — bukan sumber
mobile/core/
mobile/config/
mobile/data/
